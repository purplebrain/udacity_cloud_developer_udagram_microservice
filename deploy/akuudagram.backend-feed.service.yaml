apiVersion: v1
kind: Service
metadata:
  name: appudagram-backend-feed
  labels:
    app: appudagram-backend-feed
spec:
  ports:
  - name: "8081"
    port: 8081
    targetPort: 80
  selector:
    run: appudagram-backend-feed
